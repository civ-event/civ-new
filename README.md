# civ-new

CIV 活动前端（PC + 移动端 H5）。

## 目录

- `vue/` — PC 端
- `mobile/CIV网页移动端/vue/` — 移动端 H5

## 开发

```bash
# PC
cd vue && npm install && npm run dev

# Mobile
cd mobile/CIV网页移动端/vue && npm install && npm run dev
```
